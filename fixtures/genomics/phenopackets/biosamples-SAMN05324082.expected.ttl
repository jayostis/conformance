# This fixture is an NCBI BioSample SRA-style record (no phenopacket markers).
# detectPhenopacket() correctly returns false. The phenopacket conformance test
# asserts detect()===false for this file rather than a byte-equal turtle round-trip.
# Source corpus included it for completeness; not a phenopacket per GA4GH spec.
